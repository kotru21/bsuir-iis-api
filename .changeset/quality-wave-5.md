---
"bsuir-iis-api": minor
---

Additive Wave 5 APIs and DX: `includeNextSchedules` opt-in for normalize/schedule reads, catalog `listAllPages`, `sideEffects: false`, JSDoc/eslint improvements, and regression/smoke coverage for catalog paging and public helpers.
