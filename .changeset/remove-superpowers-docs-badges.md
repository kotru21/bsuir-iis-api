---
"bsuir-iis-api": patch
---

Remove internal `docs/superpowers` plans/specs, add npm/CI/Node/license badges to the README, and tighten local lint/format hygiene (ignore `.worktrees`/`.cursor`, enforce LF via `.gitattributes`).
