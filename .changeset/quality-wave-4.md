---
"bsuir-iis-api": patch
---

Internal foundation cleanups and DX: shared Spring page fetcher, shared freeze/auditory helpers, move schedule filter into helpers, export `AnnouncementReadOptions`, and README corrections (empty non-JSON body is `null`; last-update removal targeted at 2.0).
